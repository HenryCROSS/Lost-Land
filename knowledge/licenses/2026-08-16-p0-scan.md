# 许可证与公告扫描 — P0 收尾

**日期**：2026-08-16
**提交**：`734dc39`
**命令**：`cargo deny check`

## 结果

```
advisories ok, bans ok, licenses ok, sources ok
```

四项全部通过。当前依赖树中**没有任何 GPL 类传染性许可、没有任何收费组件**。

## 白名单（`deny.toml`）

MIT · Apache-2.0 · Apache-2.0 WITH LLVM-exception · BSD-2-Clause · BSD-3-Clause ·
ISC · Zlib · Unicode-3.0 · OFL-1.1

`ISC` / `Zlib` / `Unicode-3.0` / `OFL-1.1` 目前尚未被任何依赖用到（扫描会报
`license-not-encountered` 警告，无害），为将来的 Fusion Pixel Font 等预留。

## 本轮处理的公告

**RUSTSEC-2026-0192**（`ttf-parser` 停止维护）——已通过 winit feature 裁剪
彻底移出依赖树，非豁免。详见 [ADR 0003](../decisions/0003-winit-dependency-policy.md)。

## 已知遗留警告（不阻断）

- `bitflags` 1.3.2 与 2.13.1 并存（`multiple-versions = "warn"`）。
  来自不同上游依赖的传递引入，无法单方面消除。

## 重要提醒

仓库目前**没有配置 git 远程**，`.github/workflows/ci.yml` 里的许可证 job
**不会在任何地方执行**。在远程建立之前：

> **每个阶段闸口必须手工实跑一次 `cargo deny check`。**

理由见 ADR 0003 的「feature 合并风险」。
