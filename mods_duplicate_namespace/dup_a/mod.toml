# 故意写错的一对 mod（P4 验收 demo，附加项）：两份清单声明了同一个
# 命名空间 "dup"，用来证明本批修掉的已知缺口——topo_sort 现在会显式
# 报错，不再静默让后发现的那一份覆盖先发现的那一份。
#
# 与 mods_missing_dependency/ 同样的理由，单独放一个目录，避免连带
# 掩盖 mods/ 下其他示例 mod 各自的失败原因。
namespace = "dup"
version = "1.0.0"
