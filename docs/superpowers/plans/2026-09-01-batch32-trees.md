# 批次 32：树木系统——默认派生，只存偏差

**日期**：2026-09-01
**分支/工作树**：`wt-trees`（`C:/Users/henry/Desktop/迷途大陆/wt-trees`），基点 `26ac4dc`
**架构依据**：[ADR 0009 默认派生，只存偏差](../../../knowledge/decisions/0009-derive-by-default-store-only-deviation.md)
**验收依据**：[ADR 0031 新能力必须有经真实 `mods/` 内容、走生产路径的端到端证据](../../../knowledge/decisions/0031-end-to-end-evidence-through-real-content.md)
**覆盖判据依据**：[ADR 0022 判据覆盖不全等于没有判据](../../../knowledge/decisions/0022-guard-coverage-gap-defeats-the-guard.md)
**原始要求**：`knowledge/handoff/2026-08-28-session-handoff.md` 第三节第 5 条
（树是**部署在地形上的物品**——可砍伐、可培植、可采果、多树种）

---

## 〇、开工基线（自己跑的，不抄任何文档）

```
bash scripts/ci/run_tests.sh
```

| 项 | 值 |
|---|---|
| 通过 | **3057** |
| 测试二进制 | **134** |
| 失败 | 0 |
| EXIT | 0 |

**第一次跑挂了，不是代码缺陷**：`os error 1455`「页面文件太小，无法完成操作」+
`E0786 failed to mmap ... .rlib`，**一条测试都没跑到**。与交接文档纪律第 8 条
（`LNK1102` 链接器内存不足）**同一类**、错误号不同的假失败。`Get-PSDrive C` 显示
剩余 **190 GB**，不是磁盘满。降到 `CARGO_BUILD_JOBS=4` 重跑即 EXIT=0。
**这条记在这里，是因为它会再发生。**

三个关键常量（grep 取的，不抄文档）：

| 常量 | 落点 | 开工值 |
|---|---|---|
| `EXPECTED_WORLD_DIGEST` | `crates/ll-world/tests/determinism.rs:411` | `11_270_479_921_196_970_914` |
| `EXPECTED_REPLAY_DIGEST` | `crates/ll-sim/tests/replay.rs:1058` | `9_888_838_904_816_377_323` |
| `EXPECTED_POPULATED_WORLD_DIGEST` | `crates/ll-game/tests/populated_determinism.rs:349` | `5_695_002_850_796_623_600` |
| `CONTENT_HASH_ALGORITHM_VERSION` | `crates/ll-mod/src/content_hash.rs:967` | `34` |
| `CURRENT_SCHEMA_VERSION` | `crates/ll-content/src/save_file.rs:192` | `6` |

---

## 一、所有者的裁定与它的代价

世界 3072×2304 ＝ 7 077 888 格。森林哪怕只占 15% 也是**一百万格以上**。
独立实体存不下也算不动 ⇒ 走 ADR 0009：

```
这一格有没有树、是什么树 = 派生(种子, 位置, 地形, 气候)   ← 零存储，用时现算
                          ⊕ 偏差(仅被砍/被种/被采过的那些格) ← 进存档
```

**所有者知情并接受的代价**：**不能给一棵没动过的树单独设属性**——它在存档里
不存在。单棵想特殊化可以（写成偏差存下来），成片不行。

这条代价要落在**代码文档**里，不是只落在本计划里（本计划会被归档，代码文档会被
下一个人读到）。落点见第十二节。

---

## 二、范围（三层 + 三条交互）

| 层 | 落点 | 进不进存档 |
|---|---|---|
| **派生层** | `ll_world::tree::derived_tree_at`（新模块） | **不进**。纯函数，输入全部已在存档里 |
| **偏差层** | `ll_world::tree::TreeDeviations` → `WorldState::trees` | **进**，schema 6 → 7 |
| **解析层** | `ll_world::tree::tree_at(world, pos, forest)` | 偏差覆盖派生，两层的唯一合流点 |
| **交互** | `Intent::TendTree { actor, pos, action }` + `Effect::SetTreeDeviation` | 意图不进存档，效果写偏差层 |
| **渲染** | `ll_game::surface_draw::tree_draws` | 不进世界状态（与批次 28 地形变体同一条） |

**不做的（本批明确不碰，理由见第十七节）**：树不挡视线、不挡移动、不改移动开销；
非 `forest` 地形上不长树也不能种树；树不掉落随机战利品；没有生长阶段（树苗→大树）。

---

## 三、派生层

### 3.1 判据

```text
derived_tree_at(seed, pos, kind, forest, world_height, band_width):
    if kind != forest { return None }                       // ① 只有 forest 长树
    if fold(H("presence"), 1000) >= TREE_DENSITY_PERMILLE { return None }  // ② 疏密
    return species_from(fold(H("species"), BAND_WEIGHT_TOTAL), band_at(pos.y))  // ③ 树种

    H(domain) = FNV1a( le64(seed) ‖ le64(pos.x) ‖ le64(pos.y) ‖ len_prefixed(domain) )
    fold(h, n) = ((h as u128 * n as u128) >> 64) as u32      // Lemire 乘法取高位
```

- **`forest` 地形保留当底图**（所有者要求原话）。判据 ① 是一次 `TerrainKind`
  相等比较，O(1)。
- **两个域字符串（`"presence"` / `"species"`）**：同一格的「有没有树」与「是什么树」
  必须互相独立。共用一个摘要再切两段位域会让密度与树种相关（低位雪崩弱那半正是
  批次 28 踩过的坑）。两次哈希各自 24 字节输入，仍是常数时间。
- **混入次序：`seed, x, y, 域名`**——域名（长字符串）**放最后**。
  **这不是风格选择，是批次 28 实测逼出来的**：FNV-1a 逐字节「异或 + 乘质数」，
  最后混进去的那几个字节没有足够轮数被摊开；而 `write_i64` 写的是小端 8 字节，
  世界坐标那种小整数**高 7 字节全是 0**，相邻两格只差最低一个字节，之后只剩
  7 轮「异或 0 再乘」。把 `y` 放最后 ⇒ 纵向条纹（批次 28 实测 32% → 47%）。
  本批**照抄那个已验证的次序**，并**自己再实测一遍**（16.2 节），不假设它会自动成立。
  〔**落地更正**：实测发现这条上界判据对本函数的另一半偏离方向完全免疫，
  已改成双侧，见 16.2 与 16.9 节第 ⑤ 条。〕

### 3.2 确定性怎么保证

- **纯函数**：输入只有 `(seed, pos, kind, forest, world_height, band_width)`，
  无状态、无时钟、无 RNG、无哈希容器（C5）。
- **不碰 `DetRng`（C3）**：派生层每帧被调用的次数取决于**这一帧画了几格**
  ——摄像机缩放、窗口大小、视野半径都会改变它。让这种量参与随机流等于把确定性
  重放交给显示器分辨率。批次 28 在同一处的裁定逐字适用。
- 哈希取 `ll_core::hashing::StateHasher`（FNV-1a），模块文档写明「完全由整数运算
  构成、由规范唯一确定，因而跨平台跨版本恒定」。**不新写一个哈希。**

### 3.3 「没有消耗随机流」怎么证（不是推理，要给证据）

1. `crates/ll-world/src/tree.rs` 全文 `grep -c DetRng` = **0**；
2. 三条黄金基准重冻后，**把偏差层那一行哈希混入关掉**，三条精确回到旧值
   （见 16.6 节四步证据的第 ② 步）——若派生层消耗了随机流，关掉偏差层
   **回不到旧值**；
3. 一条断言：同一格连算两次结果相同，且**中间穿插一万次别的格子**的派生调用
   （若有隐藏可变状态/随机流，穿插会改变第二次的结果）。

### 3.4 O(1) 怎么证

- 结构上：两次定长哈希（各 24 字节输入）+ 两次乘法折算 + 一次 `TerrainKind` 比较。
  **没有循环、没有分配、没有查表遍历**。
- 判据上：`ll_world::tree` 的**生产代码**不允许出现 `while`，`for` 至多 2 处
  （由一条读源码的断言钉住，与仓库既有的 `check_single_anchor_impl.sh` 同一手法）。
  〔**落地更正**：计划原文写的是「不允许出现 `for`/`while`/`iter()`」，
  落地时发现生产代码本来就要两处 `for`（权重扫描、偏差表遍历），
  改成「`while` 禁用、`for` 至多 2 处」。〕

  **这条判据的局限如实登记**：它证的是「本函数体内没有循环」，不是「渐近 O(1)」。
  真正的 O(1) 论证是结构性的（上一段），这条断言只防**回潮**（下一个人往里加一个
  「扫周围 8 格」的循环）。

### 3.5 环面

**一行取模都不写。** 入参类型是 `ll_core::torus::TorusPos`：字段私有、只能经
`TorusSize::wrap` 构造，不变式是坐标恒被规范化到 `[0,w) × [0,h)`。
**绕回这件事在类型边界上就已经做完了**——环面上同一个物理格子，无论从哪个方向
走到它，`TorusPos` 都是同一个值，因此派生结果必然相同。

唯一的例外是 `climate::band_at(y, ..)`，它**刻意**接受未环绕的原始 `y`
（见该函数文档）——本批传进去的是 `pos.y()`，已经规范化，落在合法域内。

一条断言钉住环面接缝：`wrap(x, y)` 与 `wrap(x + w, y + h)` 的派生结果相同。

---

## 四、偏差层

### 4.1 形状

```rust
// crates/ll-world/src/tree.rs
pub enum TreeSpecies { Oak, Pine, Palm }          // 引擎侧枚举，见第五节

pub struct TreeDeviation {
    /// 现在这一格站着什么树；`None` = **被砍掉了**（派生说有、现在没有）。
    pub species: Option<TreeSpecies>,
    /// 最近一次采果的世界时刻；`None` = 从未采过（果子按派生挂着）。
    pub harvested_at: Option<Tick>,
}

pub struct TreeDeviations { by_pos: BTreeMap<TorusPos, TreeDeviation> }
```

- **`BTreeMap` 不是 `HashMap`**（C5）：这份表要参与 `WorldState::hash()` 的遍历，
  哈希容器的桶序会让同一逻辑状态产出不同摘要。`TorusPos` 已派生 `Ord`
  （`crates/ll-core/src/torus.rs:96`）。
- **`species: Option<..>` 而不是另开一个 `Felled` 变体**：「这一格现在站着什么」
  是一个问题，答案要么是某种树要么是没有。两个字段（`felled: bool` + `species`）
  会造出 `felled: true, species: Some(Oak)` 这种无意义组合。
- **一格一条记录**，砍→种→采共用同一条：一格被反复动过不会累积多条。
- `write_hash` 写在 `tree.rs` 自己身上（`FactionTable::write_hash` 的先例），
  **不往 `state.rs` 里堆**——那个文件已经在行数棘轮快照里（2023 行）。

### 4.2 解析层

```rust
pub fn tree_at(world: &WorldState, pos: TorusPos, forest: TerrainKind) -> Option<Tree>
pub struct Tree { pub species: TreeSpecies, pub fruit_ready: bool }
```

偏差**覆盖**派生：查到偏差记录就用它（`species: None` ⇒ 这里没有树，**派生说有也没有**），
查不到才现算。果子：`harvested_at` 为 `None` ⇒ 有果；否则
`world.clock >= harvested_at + FRUIT_REGROW_TICKS` 才重新有果。

### 4.3 存档

- `WorldState::trees` 是**存档主体新字段** ⇒ `CURRENT_SCHEMA_VERSION` **6 → 7**。
  `scripts/ci/check_save_schema_version.py` 会拦，照它的提示做，**不绕过**。
- **字段加在 `WorldState` 与 `WorldStateRepr` 的末尾**：postcard 按声明顺序吃字节，
  加在中间会让后续字段的字节被错位读成**合法值**（静默数据损坏）。
- **不写 `#[serde(default)]`**：postcard 非自描述，它在那条路上是**空操作**
  （见 `check_save_schema_version.py` 头注释里那条实测）。**不写没实测过的兼容性声明。**
- **老存档实测**（要跑，不许推断）：
  1. 一份 schema 6 的老存档读回来会怎样——照批次 3 的两条写法验**具体错误**，
     不是「大概会拒绝」；
  2. 「版本不对就明确拒绝」那三条既有测试仍绿；
  3. 结论如实写进 16.3 节，**不写迁移**（与交接文档第〇之二第 9 条同一条策略）。

---

## 五、树种：内容侧 vs 引擎侧

照批次 28 地形变体那次的三选一分析做，**两条路的代价先写下来再选**：

| 路 | 长什么样 | 代价 | 动内容哈希吗 |
|---|---|---|---|
| **甲：内容侧声明** | 新开 `mods/lostland/trees.json5` + `ll-mod` 的 schema / 内容哈希写入器 / 注册表 / 装载器 / 审计 | 一整套新内容表（schema、`write_*_fields`、`content_audit`、`ContentIndex` 一路带到 `ll-world` 与 `ll-game`）。`CONTENT_HASH_ALGORITHM_VERSION` 必升。`check_field_consumers.py` 要求每个新字段有决策层消费者。**真收益只有一条**：mod 作者能自带树种——**而今天没有任何 mod 需要它**（YAGNI）。更要命的是**派生层会被迫依赖注册表**：`derived_tree_at` 是每帧上千次的纯函数，让它去查一张 `ContentIndex` 表意味着它要么持有注册表引用、要么被迫变成有状态的东西 | **会** |
| **乙（选它）：引擎侧静态表** | `ll_world::tree::TreeSpecies` 三变体 + 一张 `3×3` 的气候权重常量表 | 与批次 28 `terrain_variant_count`、与 `layout::terrain_entry_name` 逐字同构（一串常量 + `match`，不经任何哈希容器，符合 C5）。mod 不能自带树种。**声明侧（`TreeSpecies`）与资产侧（三张贴图）是两份清单，会漂**——漂了当场红，两个方向都锁：`atlas_coverage.rs` 的「每一种树在真实图集里都查得到条目」与「图集里不许有声明侧数不出来的树贴图」 | **不会** |

**选乙。** 与批次 28 同一条理由，且本批多一条：派生层必须是无依赖的纯函数。
**留给「mod 真的要自带树种」那天再走甲**，那时 `TreeSpecies` 的文档就是落点。

**产出物走内容侧**（这一半必须是内容）：砍下来的木料、采下来的种子是**物品**，
物品有内容表，一律追加在 `mods/lostland/items.json5` **末尾**（插中间会让
`ContentIndex` 整体平移，本会话已因此返工过一次）。

### 5.1 分布受什么影响

- **地形**：只有 `forest` 长树（判据 ①）。这一半**是退化的**（只有一种地形长树），
  如实登记，不假装两个轴都在起作用。
- **气候**：`ClimateBand`（`ll_world::climate::band_at`，已落地）决定**树种权重**：

  | 气候带 | Oak | Pine | Palm |
  |---|---|---|---|
  | `Hot`（干热带） | 3 | 0 | 13 |
  | `Temperate`（温带） | 11 | 4 | 1 |
  | `Polar`（极地带） | 2 | 14 | 0 |

  权重和恒为 16（折算基数）。**刻意不是 1:1 硬映射**：硬映射会让「树种受气候影响」
  这条判据退化成「查一次 `match`」，测不出权重表有没有被接上；权重混合让每条带都是
  **可测的比例**，第十节给实测数字。**含 0 权重**（热带无松、极地无棕榈）
  ——那是判据能咬住的地方：把权重表改成全相同，「三条气候带的树种分布互不相同」当场红。

---

## 六、交互：砍 / 种 / 采

**一个意图变体带一个动作枚举**，不是三个意图（`Intent::Trade { direction }` 的先例）：

```rust
Intent::TendTree { actor: EntityId, pos: (i32, i32), action: TreeAction }
enum TreeAction { Fell, Harvest, Plant }
```

一条 `resolve_tend_tree`，三条动作共用前置闸门 ⇒ 少一份漂移面。

### 6.1 闸门（任何一道不过 = **零效果**，与既有五道闸门同一形状）

共用：① `actor` 存在且活着；② `pos` 够得着（与 `Intent::PickUp` 同一条范围判据，
**不另写一份**）；③ 目标格地形是 `forest`。

| 动作 | 额外闸门 | 效果 |
|---|---|---|
| `Fell` | 那一格现在真的有树 | `SetTreeDeviation{species: None, harvested_at: None}` + 木料进背包 + `ScheduleNext` |
| `Harvest` | 有树 **且** 果子已长好 | `SetTreeDeviation{species: 保持, harvested_at: now}` + 种子进背包 + `ScheduleNext` |
| `Plant` | 那一格现在**没有**树 **且** 背包里有种子 | 消耗一颗种子 + `SetTreeDeviation{species: 该地气候的派生树种, harvested_at: None}` + `ScheduleNext` |

### 6.2 产出（两件新物品，追加在 `items.json5` 末尾）

| id | 从哪来 | 到哪去 |
|---|---|---|
| `lostland:timber`（木料） | 砍伐 | 材料，可堆叠、无耐久 |
| `lostland:tree_seed`（树种） | 采果 | 培植消耗 |

**只有两件，闭环**：采 → 得种 → 种 → 长树 → 砍 → 得木料。
「宁可少而扎实」：三条路全部走通，而不是发明五种果子没人验过。

**树种决定木料产量**（`Oak 3 / Pine 2 / Palm 1`）——这是「多树种」在玩法上
**唯一真实的差异**，也是判据能咬住的地方（把三个数字改成一样，
「不同树种砍出的木料数量不同」当场红）。

**种子不分树种**：种下去长出什么由**那块地的气候**决定，不由种子决定。
这与「分布由气候决定」是同一条规则的两次应用，不是两套逻辑。

### 6.3 结算侧怎么拿到 `forest` 与两个物品索引

新开一条**窄接口** `TreeCatalog`（`forest_kind()` / `timber_def()` / `seed_def()`），
挂进 `ResolveCatalogs`，空实现 `NoTrees` 全返回 `None` ⇒ **不接这一路时树意图零效果**。
这是仓库既有的依赖倒置手法（`SkillCatalog`/`QuestCatalog`/`ContentIdLookup` 同一套），
**不是新发明**，也不给 `resolve_*` 那一串入口加参数。

### 6.4 UI

`InteractTarget::Tree { species, action }` 三支（Fell / Harvest / Plant），
走既有的交互列表。名字走 HUD 文案键（`hud-interact-tree-oak` 等三条），与
**门那两条键同一档**——树不是内容物品，没有 `display_name_key`
（理由与 `interact_target_name` 里门那一段逐字同源）。

---

## 七、贴图

三张，`tools/ll-artgen` 生成（**不手工画、不复制文件改颜色**——风格一致必须是
构造上保证的）。新模块 `tools/ll-artgen/src/tree.rs`，走 `LooseOnlyEntry`
（**追加在 `loose_only_entries()` 最末尾**，前面每一条的文件名与清单次序因此逐字不变），
`draw_entry` 加三支。

- 底色留空（画在 `Layer::DECOR`，铺满不透明底色会被读成「地形变了」）；
- 三种树**一眼分得开**：橡树宽冠圆叶、松树尖塔、棕榈细杆散叶——靠**轮廓**
  区分为主、颜色为辅（与家具那六张同一条判据）；
- 覆盖**逐张不是逐类**（批次 28 立的判据）：`atlas_coverage.rs` 三条断言
  ——每一张查得到条目、每一张不是空图、三张两两之间至少四分之一像素不同。

---

## 八、i18n

en 与 zh-CN **都加，加在文件末尾**：

| 键 | 用途 |
|---|---|
| `hud-interact-tree-oak` / `-pine` / `-palm` | 交互行的名字 |
| `hud-interact-action-fell` / `-harvest` / `-plant` | 交互行的动作 |
| `item-timber-display_name` / `item-tree_seed-display_name` | 两件新物品 |

分类表（`crates/ll-ui/tests/i18n_text_width.rs`）：`hud-interact-` 与 `item-`
两条既有规则**按最长前缀优先已经覆盖**这八条 ⇒ 本批**不加新规则**。
**这条要实测确认**（跑 `每一个键都声明了自己画在哪块面板里` 与
`分类表里没有一条死规则` 两条，两个方向都要绿），不能靠读代码断言。

---

## 九、三条黄金基准

`WorldState::hash()` 末尾加 `self.trees.write_hash(&mut hasher)`
（空表也写一个长度 0 标记——否则「没有偏差」与「偏差恰好编码成空」不可区分）
⇒ **三条基准几乎必然全部重冻**（三条都调 `WorldState::hash()`）。

**四步，一条都不能省**：

1. 确认基线红（记下 left/right，并确认 panic 落在摘要断言那一行，
   **`populated_determinism.rs` 的七条存在性断言在它之前全部通过**）；
2. **把改动关掉，确认精确回到旧值**——只注释掉 `write_hash` 那一行
   （`ll_world::tree` 整个模块、`Intent::TendTree`、`Effect`、UI、贴图全部保留），
   三条必须精确等于旧常量。**这一步是全批最重要的一步**：批次 3 正是靠它抓到
   「新地形插在中间造成 `ContentIndex` 整体平移」；
3. 恢复；
4. 新常数在**两个独立进程**复现。

**若哪一条没红**：不许当作「改动无害」——必须给**至少两组对照证伪**，
照批次 31 那三组的写法（注入点**必须在生产代码或真实内容里**，
**不能写在 `#[cfg(test)]` 模块内**——本会话有一次注入点在 `cfg(test)` 里，
结论与事实完全相反）。**一组不够**：批次 31 用第三组才区分开
「哈希看不见它」与「看得见但这次没变」。

**七条存在性断言红了不要重冻，更不许删断言让它变绿。**

---

## 十、视觉基准

`crates/ll-game/tests/visual_baselines.rs` 三张图逐像素比对。
`surface_preview` 那张走 `surface_draws` + 一圈地形——树一旦画出来，
**那张图预计会变**（取决于 `WORLD_SEED = 20260826` 下预览那 15×11 格里有没有森林）。

- **先跑，看它红不红**，再决定；
- 红了**逐张说明每处差异对应什么**（哪一格、什么树、为什么该出现在那儿），
  **不许无说明地 bless**；
- 另外两张（`settlement_preview` 走硬编码 `FLOOR_PLAN`、`npc_roster_preview`
  没有地形）**预期不变**——若它们也变了，说明树漏进了不该去的地方，**停下来查**。

**树的渲染只有一处实现**：`surface_draw::tree_draws(world, tiles, forest)`，
生产路径（`app/surface.rs`）与基准测试**调同一个函数**，不各写一遍（ADR 0021）。

---

## 十一、门禁与棘轮

| 门禁 | 预期 |
|---|---|
| `check_save_schema_version.py` | **会红**（主体形状变了）⇒ 升 `CURRENT_SCHEMA_VERSION` 到 7 并 `--bless` 形状快照 |
| `check_field_consumers.py` | 新字段都有决策层消费者（`resolve_tend_tree` / `tree_at` / `tree_draws`），预期绿 |
| `check_file_size_budget.py` | `state.rs`（2023）、`apply.rs`（1182）、`remap.rs`（1352）三个**快照内文件**会各涨几行 ⇒ `--bless` 并在 `reason` 里追加本批那几行是什么、为什么不可拆 |
| `check_i18n_strings.py` | 不许硬编码用户可见字符串 |
| `check_no_manual_euclidean_distance.sh` / `check_single_anchor_impl.sh` | 不碰 |
| `check_no_examples.sh` | **不新增 example target**（ADR 0030） |

**新代码本身一律不进快照**（`tree.rs` 等新文件必须 < 800 行）。

---

## 十二、所有者那条代价写进哪里

不只写在本计划里（计划会被归档）。三处**代码文档**，互相指向：

1. `crates/ll-world/src/tree.rs` **模块文档**——主落点，写全：为什么派生、
   代价是什么、什么时候会失效；
2. `TreeDeviations` **类型文档**——「想给一棵树设属性，就得给它写一条偏差记录；
   成片不行」；
3. `WorldState::trees` **字段文档**——指回模块文档。

---

## 十三、ADR 0022 反例验证计划

**每条新断言都要用故意改坏实现的方式验证真的会红，并确认红的原因是我以为的那个。**
**写断言之前先跑基线。** 本会话已出现 18 次「测试全绿但保护不存在」，五个形状主动防：

| 形状 | 本批哪里会踩 | 怎么防 |
|---|---|---|
| 空 `Catalog` 回落到另一门语言 ⇒「两种语言都有文案」用「文案 != 键名」判会**恒绿** | 八条新文案 | 用真实 `assets/locales`，断言 **en 文案 != zh 文案** |
| 断言恒绿因为**被断言的对象根本不存在** | 「森林里真的有树」「分布受气候影响」 | **先断言对象存在**（这一片真的是 forest、真的数出了树） |
| **判据适用面被新代码绕过** | 加第四种树时旧测试不覆盖新的那支 | 树种断言**遍历 `TreeSpecies` 全部变体**，不点名三个 |
| **一条断言前面有更容易红的断言，导致它永远轮不到执行** | 交互三条路 | 每条反例**逐个二进制单独跑**，确认红的确实是想验的那一条 |
| **生产数据恰好让判据退化成恒真** | 密度阈值、权重表 | 实测数字入档；权重表全改相同必须红 |

**还有一条观察方式的坑（批次 31 实测）**：`cargo test` 一次跑多个测试二进制时，
**第一个失败会让后面的结果看不见**。**验反例时逐个二进制单独跑。**

**点名要验的四条**（任务书指定）：

| # | 要验什么 | 怎么改坏 | 预期红在哪 |
|---|---|---|---|
| ① | 派生层确定 | 让 `derived_tree_at` 混入一个全局计数器 | `同一格连算两次结果相同` |
| ② | 偏差真的覆盖派生（砍掉的树不长回来，**存读一轮之后仍然不在**） | 让 `tree_at` 先查派生、查不到才查偏差（次序反过来） | `砍掉的树存读一轮之后仍然不在` |
| ③ | 派生层没有消耗随机流 | 在 `derived_tree_at` 里插一次 `DetRng::for_entity` 取数 | 三条黄金基准 + `不消耗随机流` 那条 |
| ④ | 树种分布真的受地形/气候影响 | 把三条气候带的权重表改成完全相同 | `三条气候带的树种分布互不相同` |

**若某条反例改坏了它不红**：不粉饰、不把断言改宽——**查清原因、如实登记、
补一条真咬得住的**。

---

## 十四、验收（ADR 0031：真实内容 + 生产路径 + 端到端）

树是**能力**，不是纯算法单元 ⇒ 三条都要满足：

- **真实内容**：端到端测试走 `ll_game::content` 的生产装载路径读仓库真实
  `mods/` 与 `assets/`，两件新物品从 `items.json5` 里读出来，不内联夹具；
- **生产路径**：从 `interact_entries` 产出的行 → `player_command` 产出的
  `Intent::TendTree` → `resolve_*` → `apply` → `WorldState`，
  **不在测试里另推一遍等价逻辑**；
- **端到端**：一路验到可观察产出——世界状态里的偏差记录、背包里的木料/种子、
  图集里查得到的贴图键、存档字节往返。

**合法例外**：`derived_tree_at` 的哈希/折算/环面那几条是引擎层纯算法单元，
不需要经 `mods/`（ADR 0031「已知的合法例外」）。

---

## 十五、提交划分（中文提交信息；尽量让每个提交自身是绿的）

1. **派生层**：`ll_world::tree` 的 `TreeSpecies` / `derived_tree_at` / 权重表 + 单元测试
   （不碰 `WorldState`，三条基准不动 ⇒ 自身绿）
2. **偏差层与存档**：`TreeDeviations` + `WorldState::trees` + `write_hash` + schema 6→7
   + 三条基准重冻 + 老存档实测
3. **交互**：`Intent::TendTree` / `Effect::SetTreeDeviation` / `TreeCatalog` / `resolve` / `apply`
   + 两件新物品 + i18n
4. **贴图与渲染**：`ll-artgen` 三张 + `tree_draws` + `atlas_coverage` + 视觉基准
5. **UI 交互列表**：`InteractTarget::Tree` + `player_command` + 端到端测试
6. **计划文档落地实测**（本文第十六节起）

**bisect 假红最常见的原因是 `cargo test` 绿但 `cargo doc` 的 rustdoc 断链红**
——每个提交前跑一次 `check_doc_links.sh`。

**不 push，不合并 main。**

---

## 十六、落地实测

### 16.1 落点一览

| 要交付的东西 | 落点 |
|---|---|
| **派生层** | `ll_world::tree::derived_tree_at` / `derived_species_at` / `position_digest` / `species_weights` |
| **偏差层** | `ll_world::tree::{TreeDeviation, TreeDeviations}` → `WorldState::trees`（字段在**末尾**） |
| **解析层** | `ll_world::tree::tree_at`（两层唯一合流点） |
| **结算** | `ll_sim::intent::Intent::TendTree` + `ll_sim::tree::{TreeAction, TreeCatalog, NoTrees}` + `ll_sim::resolve::tree::resolve_tend_tree` + `ll_sim::effect::Effect::SetTreeDeviation` + `apply.rs` 一支 |
| **内容索引解析** | `ll_mod::tree::RegisteredTrees`（`lostland:forest` / `lostland:timber_log` / `lostland:tree_seed`） |
| **生产接线** | `ll_game::content::RuntimeCatalogs`（`trees:` 那一行） |
| **内容** | `mods/lostland/items.json5` 末尾两条；`assets/locales/{en,zh-CN}.ftl` 各八条（**加在文件末尾**） |
| **贴图** | `tools/ll-artgen/src/tree.rs` 三张 + `TREE_NAMES` + `draw_entry` 三支 |
| **渲染** | `ll_game::surface_draw::{tree_draws, tree_sprite_key, TREE_SPRITE_PREFIX, TREE_ENTITY_BASE}`；生产调用点 `ll_game::app::surface` |
| **UI** | `InteractTarget::Tree` + `tree_rows`；`TalkLookup` 更名 `InteractLookup` 并加两个字段 |
| **测试** | `ll-world/tests/tree_derivation.rs`（9）、`ll-world/tests/tree_deviation.rs`（5）、`ll-sim/tests/tend_tree.rs`（11）、`ll-game/tests/tree_end_to_end.rs`（6）、`atlas_coverage.rs`（+3） |

### 16.2 派生层

**判据**：① 只有 `forest` 地形长树；② `fold(H("presence"), 1000) < 620`；
③ 树种按 `ClimateBand` 的 3×3 权重表加权抽取。

**确定性**：纯函数，输入只有 `(seed, pos, kind, forest, world_height, band_width)`。
无状态、无时钟、无 RNG、无哈希容器。

**没有消耗随机流的证据（三条，都是实测）**：

1. `crates/ll-world/src/tree.rs` **代码行**（剔除注释后）里 `DetRng` 零命中
   ——由 `派生层里没有循环也没有随机流` 执行。
   〔第一版直接扫全文，**当场红在自己的模块文档上**（那段文档解释的正是
   「为什么不碰 `DetRng`」）——判据把讲这件事的散文当成了做这件事的代码。
   已改成只扫代码行，教训记在那条测试的注释里。〕
2. **黄金基准重冻的第 ② 步**：只把 `WorldState::hash()` 里
   `self.trees.write_hash(..)` 一行注释掉，三条基准**精确回到旧值**。
   派生层在这一步里完整存在并可被调用——它若动过 `DetRng` 或往世界状态里
   写过任何东西，关掉一行哈希混入是回不到旧值的。
3. `同一格连算两次结果相同`，且**中间穿插一万次别的格子**的派生调用。

**O(1) 的证据**：结构上是两次定长哈希（各 24 字节输入）+ 两次乘法折算 +
一次地形比较 + 一段最多三次迭代的定长权重扫描，没有循环、没有分配。
判据上由 `派生层里没有循环也没有随机流` 禁止 `while` 并把 `for` 限制在
2 处。**这条判据的局限如实登记**：它证的是「没有回潮」，不是渐近复杂度。

**环面**：本模块**一行取模都没写**。入参是 `TorusPos`（字段私有、只能经
`TorusSize::wrap` 构造），绕回在类型边界上就已经做完。
`环面接缝上同一个物理格子派生出同一棵树` 钉住它。

**相邻独立性实测**（512×512，种子 4242）：

| 混入次序 | 横向 | 纵向 | 斜向 |
|---|---|---|---|
| **`seed, x, y, 域名`（落地采用）** | 531‰ | 537‰ | 537‰ |
| 域名放在 `y` 之前（`y` 最后） | 530‰ | **501‰** | 532‰ |
| 域名放在 `x` 之前（`x` 最后） | **501‰** | 537‰ | 531‰ |

相互独立时应为 529‰（= p² + (1−p)²，p = 620‰）。
**与批次 28 的方向相反**——那里是「三张变体挑一张」，弱雪崩表现为相邻格
更常取到同一张（32% → 47%）；这里是二值阈值折算，同样的弱雪崩表现成相邻格
更常落在阈值**两侧**。判据因此必须是双侧的，见 16.9 节第 ⑤ 条。

### 16.3 偏差层与存档

**形状**：`WorldState::trees: TreeDeviations`，内部
`BTreeMap<TorusPos, TreeDeviation>`；`TreeDeviation { species: Option<TreeSpecies>,
harvested_at: Option<Tick> }`。一格一条记录，砍/种/采共用。

**schema：6 → 7。** `check_save_schema_version.py` 实跑报出
`WorldState`/`WorldStateRepr` 字段序 16 → 17，且
`TreeDeviations`/`TreeDeviation`/`TreeSpecies` 三个类型新进入闭包。
升版后 `--bless`，复跑绿。

**老存档实测行为（两条，都跑过，不是推断）**：

1. `crates/ll-content/src/save_file.rs::树木批次之前的老存档被明确拒绝而不是静默按新布局误解析`
   ——一份 schema 6 的存档读回来走
   `LoadError::SchemaMigrationGap { from: 6 }`，**在版本比较那一步就明确
   拒绝，根本走不到主体解析**。
2. `crates/ll-world/tests/tree_deviation.rs::砍掉末尾树木字段的旧字节流用postcard解不回新形状`
   ——**直接对着字节流**：空偏差表恰好编码成一个字节，砍掉它就是加
   `trees` 之前的那份字节流，解码报的**具体错误**是
   `postcard::Error::DeserializeUnexpectedEnd`，不是静默误解析。
   带对照组（完整字节流往返无损、世界摘要相同）。

**「老存档里没有树的偏差，读回来会怎样」的答案**：**读不回来**——它在
版本比较那一步就被拒了。**不写迁移**（交接文档第〇之二第 9 条既有策略）。
`WorldStateRepr::trees` 上的 `#[serde(default)]` **只服务本文件内部的
`serde_json::json!` 测试固件**，在 postcard 这条真正的主体路径上是空操作，
本批**没有据此声称任何兼容性**。
`ll-content` 那三条「版本不对就明确拒绝」的既有测试仍绿（112 条全过）。

### 16.4 树种：选了引擎侧

两条路的代价对照写在第五节与 `TreeSpecies` 的类型文档里。**选引擎侧**，
除了批次 28 那两条理由，本批多一条**致命的**：`derived_tree_at` 是每帧
上千次的纯函数，让它查 `ContentIndex` 表就毁掉「无依赖纯函数」这条性质。

**分布受什么影响**：地形（只有 `forest` 长树，**这一半是退化的**，如实
登记）+ 气候带（`ClimateBand` 决定树种权重）。

**实测分布**（种子 4242，512×512 全森林）：

| 气候带 | Oak | Pine | Palm | 棵数 |
|---|---|---|---|---|
| `Hot` | 187‰ | **0‰** | 812‰ | 39 987 |
| `Temperate` | 687‰ | 249‰ | 62‰ | 82 641 |
| `Polar` | 123‰ | 876‰ | **0‰** | 39 977 |

与权重表（3/0/13、11/4/1、2/14/0，和恒 16 ⇒ 187.5/0/812.5、687.5/250/62.5、
125/875/0）逐条吻合。**密度实测 620‰**（设计值 620‰）。

### 16.5 交互：砍 / 种 / 采各自做到什么程度

| 动作 | 做到什么程度 |
|---|---|
| **砍伐** | 树从世界上消失（偏差记录 `species: None`），木料进背包，**数量由树种决定**（Oak 3 / Pine 2 / Palm 1），消耗一个回合 |
| **采果** | 树**留着**，种子进背包，`harvested_at` 记下时刻，果子过 `FRUIT_REGROW_TICKS`（86400 tick = 一天）才重新长好，消耗一个回合 |
| **培植** | 消耗一颗种子，在**一格没有树的森林**上长出一棵，**树种由那块地的气候决定**（与派生层同一个 `derived_species_at`），新树的果子要等一个周期，消耗一个回合 |

闭环：采 → 得种 → 种 → 长树 → 砍 → 得木料。**两件新物品**（`timber_log` /
`tree_seed`），不是六件——「宁可少而扎实」。

**三条路都从交互列表走到世界状态**，端到端证据在
`crates/ll-game/tests/tree_end_to_end.rs`（真实 `mods/` + 真实 `assets/` +
生产装载路径 + `interact_entries` → `player_command` → `TurnEngine`）。

### 16.6 三条黄金基准：**全部重冻**，而且本批**重冻了两次**

| 基准 | 开工值 | 第一次（哈希多混一张空表） | 第二次（内容多两条） |
|---|---|---|---|
| `EXPECTED_WORLD_DIGEST` | `11_270_479_921_196_970_914` | `9_641_812_801_195_310_050` | 不动（不装载 `mods/`） |
| `EXPECTED_REPLAY_DIGEST` | `9_888_838_904_816_377_323` | `8_626_273_720_694_163_787` | 不动（同上） |
| `EXPECTED_POPULATED_WORLD_DIGEST` | `5_695_002_850_796_623_600` | `5_903_614_475_130_246_384` | `10_411_288_278_210_867_400` |

**两次都跑满四步**，证据写在各自常量的文档注释里。**没有任何一条是「没动」**，
因此不需要对照证伪。

**第二次重冻的第 ② 步抓到一处真缺陷**——这一步存在的全部理由：见 16.10 节。

**七条存在性断言：仍然全绿。** 两次基线红时 panic 都落在
`assert_eq!(world.hash(), ..)` 那一行（`populated_determinism.rs:415` /
`:441`），在七条**之后**——世界没有变空。一条都没重冻、一条都没删。

### 16.7 内容哈希、存档 schema 与行数棘轮

- `CONTENT_HASH_ALGORITHM_VERSION`：**34，不动**。本批没有改内容 schema、
  没有给任何表加字段、没有给任何枚举加变体（树种是**引擎侧**枚举，不进
  内容哈希算法）。追加两条内容会改变**算得出来的哈希值**，但那不是
  ADR 0027 意义上的「算法变了」。
- `CURRENT_SCHEMA_VERSION`：**6 → 7**，形状快照已 `--bless`。
- 行数棘轮 `--bless` 四处，`reason` 各自追加了本批加了什么、为什么不可拆：
  `remap.rs` +1、`save_file.rs` +16、`state.rs` +7、`apply.rs` +3。
  **新代码全部在新文件里**（`ll-world/src/tree.rs`、`ll-sim/src/tree.rs`、
  `ll-sim/src/resolve/tree.rs`、`ll-mod/src/tree.rs`、`ll-artgen/src/tree.rs`），
  没有一个进快照。

### 16.8 视觉基准：**只有 `surface_preview.png` 变了**

另两张（`settlement_preview.png` / `npc_roster_preview.png`）**逐字节未变**
——与预案一致（前者走硬编码 `FLOOR_PLAN`，后者没有地形）。

**逐处说明 `surface_preview.png` 的差异**（22 592 / 479 232 像素，4.7%）：

差异图（洋红）显示**全部差异落在一块 6 格的 L 形区域内**，与
`arrange_surface_scene` 里补的那片林子逐格对应，画布其余部分一个像素都没动。
那 6 格是 `(px-3,py-3)`、`(px-2,py-3)`、`(px-1,py-3)`、`(px-3,py-2)`、
`(px-1,py-2)`、`(px-3,py-1)`。

| 差异 | 对应什么 |
|---|---|
| 6 格地形从原本的地形换成森林 | 那 6 格被 `set_terrain` 铺成了 `forest` |
| 其中 3 格叠上树（2 棵棕榈 + 1 棵橡树） | 派生层在那 3 格算出了树（密度 620‰ 下 6 格出 3~4 棵，落在预期内）。坐标与树种实测：`(236,2299) palm`、`(237,2299) oak`、`(235,2300) palm` |
| 上限核对 | 6 格 × (16×16 像素 × ZOOM 4²) = 24 576 像素，实测 22 592 = 91.9%，差的那部分是新旧贴图**恰好同色**的像素与树贴图的透明区 |

**这片林子是补上去的，理由要写清**：第一版把 `tree_draws` 接进基准却
一棵树都没画出来（实测 `TREEDRAWS=0`——种子 20260826 的出生点附近一格森林
都没有），那张基准对树的覆盖是**空的**却照样全绿。补林子的同时加了一条
「一棵树都没画出来就红」的前置断言，反例验证见 16.9 节第 ⑬ 条。

### 16.9 ADR 0022 反例验证：十五条实测

每一条都**先跑基线**（绿），再改坏，**逐个测试二进制单独跑**（批次 31 那条
「一次跑多个二进制时第一个失败会让后面看不见」的教训），确认**红的是想验的
那一条**，最后还原。

| # | 改坏什么 | 结果 | 红在哪（点名断言） |
|---|---|---|---|
| ① | **派生层确定**：让 `position_digest` 混入一个全局调用计数器 | **红** | `同一格连算两次结果相同`：`left: Some(Oak)` / `right: None` |
| ② | **偏差覆盖派生**：把 `tree_at` 的查询次序反过来（先派生、查不到才查偏差） | **红** | `砍掉的树存读一轮之后仍然不在`：「砍掉之后这一格不该还有树——偏差没有覆盖派生」 |
| ③ | **派生层消耗随机流**：在 `derived_tree_at` 里插一次 `DetRng::for_entity` | **红** | `派生层里没有循环也没有随机流` |
| ④ | **树种分布**：把三条气候带的权重表改成完全相同 | **红**（详见下方「改坏了它不红」） | 最终红在 `三条气候带的树种分布互不相同` 的比例判据上 |
| ⑤ | **哈希混入次序**：把 `y`（或 `x`）挪到域名之后 | **红**（详见下方） | `相邻格子长树与否的相关性接近相互独立`：偏离 28‰ > 门槛 20‰ |
| ⑥ | `TreeSpecies::timber_yield` 三支改成同一个数 | **红** | `不同树种砍出的木料数量不同`：「实测 [(Oak,2),(Pine,2),(Palm,2)]」 |
| ⑦ | 采果那一支写成 `TreeDeviation::felled()` | **红** | `采一次果树还在种子到手`：「采完果树该还在」 |
| ⑧ | 删掉 `resolve_tend_tree` 的 `Effect::ScheduleNext` | **红** | `砍种采三条路都消耗一个回合` |
| ⑨ | 从 `TreeSpecies::ALL` 删掉 `Palm`（图画了、声明没加） | **红** | `图集里不许有声明侧数不出来的树贴图`：点名 `lostland:tree_palm` |
| ⑩ | 把 `sprite_stem` 的 `Palm` 改成不存在的名字（声明了、图没画） | **红** | `每一种树在真实图集里都查得到条目` |
| ⑪ | **列表与结算不一致**：列表侧不判果子长好没有 | **红** | `玩家从交互列表采果之后种子真的进了背包` 的后半段（采完那一行不该还在） |
| ⑫ | **生产接线**：`ll-game` 那一行 `trees` 换成 `NoTrees` | **红** | `玩家从交互列表砍倒一棵树木料真的进了背包`：「Fell 走完生产链之后什么都没发生——接线断了」 |
| ⑬ | 让 `tree_draws` 恒返回空 | **红** | `地表内容预览与基准逐像素一致`：「预览场景里一棵树都没画出来——这张基准对树的覆盖是空的」（另两张基准仍绿） |
| ⑭ | 培植那一行的种子闸门：列表侧不查背包 | **红** | `没有种子时培植那一行根本不出现` |
| ⑮ | `resolve` 侧不判目标格是不是森林 | **红** | `非森林地形上三条路全都零效果`：「草地上的 Fell 该零效果」 |

#### 「改坏了它不红」：两条，都查清原因并补了真咬得住的判据

**这两条是本节最有价值的部分。** 按任务书要求：不粉饰、不把断言改宽。

**第 ④ 条第一次跑：`assert_ne!(r[i], r[j])` 是一条近似恒真的断言。**
把权重表三行改成完全一样之后，三条带的比例实测是
`[688,249,62]` / `[687,249,62]` / `[687,249,62]`——**最大差只有 1‰，
`assert_ne!` 照样过**。三条带的树数量不同，抽样噪声本来就让千分比整数彼此
不等。当时真正红的是它后面那两条零权重断言（「干热带不该有松树」），
也就是说**判据的主体部分在空跑**，只是碰巧被邻居救了。
改成「两两之间最大差 ≥ 100‰」（真实权重下差值都在 500‰ 以上），
把那两条零权重断言临时关掉复验，同一个改坏动作当场红在新判据上。

**第 ⑤ 条第一次跑：只有上界的判据对另一半方向完全免疫。**
第一版照抄批次 28 写成「比例 < 理想 + 8 个百分点」。把 `y` 挪到域名之后
**没红**：纵向 501‰，比理想的 529‰ **低** 28‰。
原因见 16.2 节——批次 28 验的是「三张变体挑一张」，本函数验的是二值阈值
折算，**同样的弱雪崩，偏离方向相反**。改成双侧 ±20‰（本次序实测偏离
2/8/8‰，改坏后 28‰），`x`/`y` 任一挪到最后都当场红。
顺带把写死的理想值 529‰ 改成从实测边际密度现算 `p²+(1−p)²`——写死的话
一改 `TREE_DENSITY_PERMILLE` 这条判据会**静默**失效。

两条的原文都在测试注释里逐字保留，写明什么时候、因为什么、结论现在是什么。

### 16.10 第 ② 步抓到的那处真缺陷：内容 id 跨表撞名

**木料第一版叫 `lostland:timber`，而 `mods/lostland/resources.json5` 里的
据点资源「木材」早就叫这个名字。**

`ll_mod::registry::Registry` 是**一个** id ↔ `ContentIndex` 空间（`intern`
共享、`define` 按表分开），于是同一个 `ContentIndex`（实测 **40**）同时是
一条物品和一条资源——`ItemTable::get(40)` 与 `ResourceTable::get(40)` 都
答得出来，「这个索引是哪张表的」再也问不清楚。

**没有任何门禁拦住它**：json5 schema 校验绿、`ll_mod::content_audit` 的跨表
引用完整性检查绿、`base_mod_items_and_crafting.rs` 的「清单不多不少」绿。

**抓到它的是一个对不上的数字**：新增两条内容，后续 `ContentIndex` 却只
平移了 **1**。探针实测：

| id | 加两件之前 | 撞名那一版 | 改名之后 |
|---|---|---|---|
| `lostland:forest`（地形，引擎侧先注册） | 4 | 4 | 4 |
| `lostland:iron_ingot`（items.json5 靠前） | 59 | 59 | 59 |
| `lostland:oak_chair`（items.json5 靠后） | 106 | 106 | 106 |
| `lostland:warrior`（classes.json5，在 items 之后） | 127 | **128** | **129** |
| `lostland:human.corpse`（引擎在全部 mod 之后注册） | 235 | **236** | **237** |

改名 `lostland:timber_log` 之后平移才是 2。这笔账记在
`crates/ll-mod/src/tree.rs` 的 `TIMBER_ID` 文档、`mods/lostland/items.json5`
的原地注释、与 `EXPECTED_POPULATED_WORLD_DIGEST` 的常量文档三处。

**已另开一条后续工单**：全仓库跨表撞名审计 + 立一道门禁（「一个
`ContentIndex` 至多被一张表 define」）。

### 16.11 门禁与测试数

| 项 | 值 |
|---|---|
| 改前 | **3057 通过 / 134 二进制**（自己跑的） |
| 改后 | **3096 通过 / 138 二进制** |
| `bash scripts/ci/run_all.sh` | **exit 0**，全 14 步 |

**偶发红**：交接文档点名的 `npc_materialization` 那条
（`surface_store.rs:656` 偶发 panic）**本批一次都没撞到**。

**撞到的是另一类假失败，两次**：

1. 开工基线第一次跑 `EXIT=101`，`os error 1455`「页面文件太小」+
   `E0786 failed to mmap ... .rlib`，**一条测试都没跑到**；
2. 门禁第一次全跑 `EXIT=101`，`crate ll_text required to be available in
   rlib format, but was not found in this form`。

两次都不是磁盘满（`Get-PSDrive C` 剩 140~190 GB），也不是代码缺陷——与纪律
第 8 条（`LNK1102` 链接器内存不足）**同一类、错误号不同**的高并行假失败。
降到 `CARGO_BUILD_JOBS=4` 重跑即 `EXIT=0`。**这两个错误号建议补进纪律第 8 条
的症状清单**（本文件是更正方，被更正方是
`knowledge/handoff/2026-08-28-session-handoff.md` 第一节第 8 条）。

### 16.11 之二、按纪律第 9 条写回的更正（每一处都两边互相指向）

| 更正方（本文件） | 被更正方 | 结论现在是什么 |
|---|---|---|
| 本文全篇 | `knowledge/handoff/2026-08-28-session-handoff.md` **第三节第 5 条**（「开工前再问一次」） | 已经问过并批准，走 ADR 0009。该条已加更正段、原文划掉保留、指回本计划 |
| 本文全篇 | 同一份 **第四节第 3 条**（「树木是派生还是每棵独立实体，仍然悬着，是这三条里唯一还开着的」） | 已裁定并落地，本条关闭；那三条「会越拖越贵」的现在全部关闭 |
| 16.11 节 | 同一份 **第一节第 8 条**（高并行下的假失败，原文只点了 `LNK1102`） | 同一类假失败还有另外两个错误号（`os error 1455` 的 mmap 失败、`required to be available in rlib format`），判据与处置完全一致。该条已加补记并指回本节 |

三处都按纪律第 9 条的三步做：更正方给出被更正方的路径与条目号，被更正方
**原文一个字不改**（划掉保留或原地追加），并**指回**本文件。

### 16.12 所有者那条代价写进了哪里

「**不能给一棵没动过的树单独设属性——它在存档里不存在。单棵想特殊化可以，
成片不行。**」三处代码文档，互相指向：

1. `crates/ll-world/src/tree.rs` **模块文档**「项目所有者知情并接受的代价」
   一节（主落点，写全了为什么、代价是什么、要推翻它推翻的是什么）；
2. `TreeDeviations` **类型文档**「想给一棵树设属性，就得给它写一条记录；
   成片不行」；
3. `WorldState::trees` **字段文档**「这里存的**不是**世界上的树」，指回
   模块文档。

---

## 十七、规格没裁定、本批临时选的做法

**这一节是本报告最重要的部分。** 每条都写明选了什么、为什么选它、
以及**怎么反转**。全部按「最保守、最容易反转」选。

1. **树不挡视线、不挡移动、不改移动开销。** `forest` 地形本身已经
   `blocks_sight`，树只是叠在上面的一层。让树挡路会改变 FOV 与寻路，
   直接动 `EXPECTED_REPLAY_DIGEST`，且会让「砍出一条路」变成核心玩法——
   那是一次玩法裁定，不是本批能顺手做的。
   **反转成本**：`tree_at` 已经是唯一的查询入口，给 `resolve_move` /
   `compute_fov` 各加一次查询即可，但要重冻回放摘要。

2. **非 `forest` 地形上不长树，也不能种树。** 「`forest` 保留当底图」是
   所有者的原话，本批按最窄的读法执行。沙漠绿洲的棕榈、草原上的孤树都
   做不到。
   **反转成本**：`derived_tree_at` 的判据 ① 与 `resolve_tend_tree` 的第
   四道闸门各改一行；不动世界摘要（树不进世界状态），只动画面与可交互格。

3. **密度 620‰、果子重生 86400 tick（一天）、木料产量 3/2/1、气候权重表**
   ——全是没有数据支撑的内容参数，取值理由写在各自的常量文档里。
   **反转成本**：改常量即可，三条黄金基准**不会**变（派生层不进世界状态）。

4. **树的产出物只有两件，且「果子」与「种子」是同一件东西。**
   单开一件果子需要食物系统才有消费者，而食物系统不在本批范围——那件果子
   会当场变成「声明了没人读」。
   **反转成本**：`items.json5` 加一条 + `TreeCatalog` 加一条查询。

5. **培植长出什么树由那块地的气候决定，不由种子决定。** 因此只有一种
   种子。反过来做（种子带树种）要给种子加一个「是什么树的种子」的字段，
   而物品表今天没有这层。
   **反转成本**：中等——要给物品加字段，动内容 schema 与内容哈希算法版本。

6. **采果之后偏差记录不回收。** 果子长好之后那条记录语义上退回「与派生
   一致」，但记录仍然留着。回收要在 `apply` 侧比对派生值，而 `apply`
   按 C1 是「一句判断都不做」的地方。
   **代价**：一个把全世界的树都采一遍的玩家会留下一百万条记录。
   **这与 ADR 0009 的适用条件第 3 条（偏移必须可以有界存储）擦边**，如实
   登记。**反转成本**：低——在 `resolve` 侧算好「这条记录是否等于派生值」，
   等于就产出一条删除效果。留给「偏差表真的涨起来」那天。

7. **三个内容 id（`lostland:forest` / `timber_log` / `tree_seed`）拼错了
   是静默失效，没有升成 `base_contract` 的硬性装载失败。**
   `base_contract` 是一份「Rust 点名引用了哪些本体内容」的清单，往里加
   东西要同时改它的错误类型与玩家可见的报错文案，属另一个批次。
   **代价换成了一条会红的断言**（`真实内容装载之后三个树木索引都解析得到`）。
   **反转成本**：低，落点写在 `ll_mod::tree` 模块文档里。

8. **树的贴图键命名空间写死成 `lostland:`**（与 `GROUND_PILE_SPRITE` /
   `NPC_SPRITE` 同一档）。树不是内容表里的一条，没有 `registry.resolve`
   那条路可走。mod 换不掉树的样子。
   **反转成本**：等树种走内容侧那天一并解决。

9. **`TalkLookup` 更名 `InteractLookup`。** 它现在还带森林地形与种子索引，
   旧名已名不副实。这是一次纯机械重命名（43 处，编译器全程盯着），
   没有行为变化。

10. **交互列表里采果排在砍伐前面。** 站在一棵挂着果子的树前，「摘一个」
    几乎总是比「砍掉它」更常见的意图。与对话那一行排最前是同一条判断。
    **反转成本**：一行。

11. **树在 `Layer::TERRAIN` 而不是 `Layer::DECOR`。** 掉在树下的东西、
    摆在树旁的家具、站在树前的人都该盖在树前面；由「层」表达这件事比在
    两个号段之间做一次没有语义的大小约定干净。
    **反转成本**：一行（改层）+ 一个号段常量。

12. **视觉基准的场景里补了一片 6 格的林子。** 不补的话那张基准对树的覆盖
    是空的（实测）。这改变了一张冻结基准的内容，理由与逐格核对写在
    16.8 节与那段代码的注释里。
