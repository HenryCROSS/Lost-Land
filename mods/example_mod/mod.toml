# 迷途大陆示例 mod：P4 验收 demo（task-12）用来证明 mod 能新增真实内容
# 并走与本体完全相同的注册路径。gameplay.scm（P5-C 缺口修补批次新增）
# 证明同一条能力现在覆盖职业/技能/副职/任务/种族，不只是地形。
#
# behavior.scm（规格 §10.5 行为树接线批次新增）示范一个真的会用技能
# 的敌人——**刻意不在 entry_points 里**，理由见该文件头部注释：它面向
# 一套不同的运行期 ScriptEngine（ll_mod::script_behavior_source），
# 不是这里列出的、供装载管线注册内容用的引擎。
namespace = "examplemod"
version = "0.1.0"
entry_points = ["terrain.scm", "gameplay.scm"]
