// 示例 mod 的加值类型——规则修正合并时「同一类型取最强、不同类型
// 相加」里的那个类型（D&D 3.5e / 开拓者的加值类型模型）。
//
// 本体（mods/lostland/modifier_types.json5）现在也有一条名册
// （lostland:gear，装备来源）。这两份名册是**并列**的，不是重复：一个
// mod 既可以引用本体已有的类型（想与本体的装备减伤竞争取最强，就写
// lostland:gear），也可以像本文件这样注册自己的——加值类型是开放注册
// 表，命名空间就是「与谁竞争」的分界线。
//
// 本 mod 的两条刻意留在 examplemod 命名空间下：软泥怪的天生耐酸与抗酸
// 护符讲的是这个 mod 自己的一套分类学（体质 vs 附魔），与本体那条
// 「穿在身上的东西」不是同一件事,不该合并成一条。
{
  modifier_types: [
    {
      // 天生：种族/体质带来的，不来自任何外物。acid_hide 走这一类。
      id: "examplemod:innate",
    },
    {
      // 附魔：装备上的魔法强化。acid_ward_amulet 走这一类。
      //
      // 与 innate 分成两类不是为了凑数：一只天生耐酸的软泥怪再戴一枚
      // 抗酸护符，两者理应叠加（体质与附魔是两回事）；而两枚同样的
      // 护符不该叠加。这正是加值类型模型要表达的区别。
      id: "examplemod:enhancement",
    },
  ],
}
