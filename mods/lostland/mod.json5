// 迷途大陆本体内容 mod。
//
// # 这不是一个「示例」，是本体自己的游戏内容
//
// 项目所有者裁定「本体主要是把整个框架做好做完整，提供足够好和足够多
// 的功能和选项」——即 Rust 侧只留**能力**（注册表、契约解析、结算引擎、
// 值哈希），**实例**（哪个种族叫什么、属性修正是多少）全部落在这里。
// 本目录因此是「本体即 Mod」原则最后一块拼图：本体内容与第三方 mod
// 走的是同一条 `register-*` 脚本通道，没有任何本体专属的特权入口。
//
// # 这个目录不能删
//
// 与 `mods/example_mod/`（可有可无的演示）不同，本目录是**强制装载**
// 的：`ll_game::content::load_content` 在装载完毕后会按 id 逐字段解析
// `ll_mod::race::BaseRaceIds` 这类本体句柄结构体（`ll_mod::base_contract`
// 的契约解析机制），任何一条本体内容缺失都会让装载整批失败并点名缺了
// 哪些 id，而不是静默进到一个残破的游戏里。误删/改名本目录的表现是
// 启动期一条可行动的错误，不是十分钟后某个 NPC 生成不出来。
//
// # 为什么按内容类型拆多个文件，而 entry_points 只有一条
//
// 拆文件是为了让「本体的种族都写在哪」有一个不需要搜索就能回答的
// 答案。但拆成多个 `entry_points` 曾经带来一个副作用：数组顺序变成
// 了真实约束（谁必须先注册），而清单里看不出为什么。模块系统落地
// 之后，入口收成一条 `main.scm`，文件之间的先后由各文件自己的
// `(require "…")` 表达——见 main.scm 文件头与下方 entry_points 上的
// 注释。
{
  namespace: "lostland",

  // **改这个版本号 = 宣布此前用本 mod 生成的全部存档作废。**
  // 项目所有者裁决：「新版本不兼容旧版本存档就是了，版本不对就打不
  // 开。」`ll_content::load_error::check_mod_set` 是硬门禁，比的是这个
  // 字符串**原样相等**，不做语义化版本解析。什么时候该动、什么时候
  // 不该动，见 knowledge/design/save-and-mod-version-policy.md。
  version: "0.1.0",

  // 内容清单（哪一类写在哪个文件里）：
  //   races.scm      种族，迁移自 `ll_mod::race::materialize_base_races`（已删除）
  //   classes.scm    职业四条，迁移自 `ll_mod::class::materialize_base_classes`（已删除）
  //                  + 上一批就迁进来的卫兵，见该文件头
  //   skills.scm     技能五条，迁移自 `ll_mod::skill::materialize_base_skills`（已删除）
  //   quests.scm     任务四条，迁移自 `ll_mod::quest::materialize_base_quests`（已删除）
  //   crafting.scm   制作类别四条，都不设副职闸门，理由见该文件头
  //   subclasses.scm 副职：四条制作类 + 各自的使用计数获得条件，外加迁移自
  //                  `ll_mod::subclass::materialize_base_subclasses`（已删除）的
  //                  剑舞者/学徒两条
  //   tags.scm       标签名册
  //
  // **entry_points 只有一条，顺序不再有语义**。此前这份数组的顺序是
  // 真实约束（crafting 必须排在 subclasses 前、classes 必须排在 skills
  // 前），而清单里看不出为什么、也没有任何机制阻止别人改坏它。现在那
  // 两条约束写成了 `(require "crafting")` / `(require "classes")`，就在
  // 需要它们的那个文件里；main.scm 里的七行 require 刻意按字母序排，
  // 与旧的工作顺序不同——顺序若还有语义，装载当场就会失败。
  //
  // 想知道模块系统的规则（同 mod 相对路径、跨 mod 必须带 mod id 前缀
  // 且先在 dependencies 里声明），见 `ll_script::modules` 模块文档与
  // knowledge/design/mod-package-structure.md。
  entry_points: ["main.scm"],
}
