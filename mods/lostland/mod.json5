// 迷途大陆本体内容 mod。
//
// # 这不是一个「示例」，是本体自己的游戏内容
//
// 项目所有者裁定「本体主要是把整个框架做好做完整，提供足够好和足够多
// 的功能和选项」——即 Rust 侧只留**能力**（注册表、契约解析、结算引擎、
// 值哈希），**实例**（哪个种族叫什么、属性修正是多少）全部落在这里。
// 本目录因此是「本体即 Mod」原则最后一块拼图：本体内容与第三方 mod
// 走的是同一条内容数据文件通道，没有任何本体专属的特权入口。
//
// # 这个目录不能删
//
// 与 `mods/example_mod/`（可有可无的演示）不同，本目录是**强制装载**
// 的：`ll_game::content::load_content` 在装载完毕后会按 id 逐字段解析
// `ll_mod::race::BaseRaceIds` 这类本体句柄结构体（`ll_mod::base_contract`
// 的契约解析机制），任何一条本体内容缺失都会让装载整批失败并点名缺了
// 哪些 id，而不是静默进到一个残破的游戏里。误删/改名本目录的表现是
// 启动期一条可行动的错误，不是十分钟后某个 NPC 生成不出来。
//
// # 为什么按内容类型拆多个文件，而清单里一个都没列
//
// 拆文件是为了让「本体的种族都写在哪」有一个不需要搜索就能回答的
// 答案。内容文件**不登记在本清单里**，靠固定文件名被发现——与矮人
// 要塞的 raws、RimWorld 的 defs 同一个形态。清单里列文件名曾经带来
// 一个副作用：数组顺序变成了真实约束（谁必须先注册），而清单里看不出
// 为什么。装载顺序现在写在 `ll_mod::content_data` 的 CONTENT_FILES
// 常量里，由引擎保证，内容作者写不错也改不动。
{
  namespace: "lostland",

  // **改这个版本号 = 宣布此前用本 mod 生成的全部存档作废。**
  // 项目所有者裁决：「新版本不兼容旧版本存档就是了，版本不对就打不
  // 开。」`ll_content::load_error::check_mod_set` 是硬门禁，比的是这个
  // 字符串**原样相等**，不做语义化版本解析。什么时候该动、什么时候
  // 不该动，见 knowledge/design/save-and-mod-version-policy.md。
  version: "0.1.0",

  // 内容清单（哪一类写在哪个文件里）：
  //   races.json5             种族三条（各带一套出生装备）
  //   classes.json5           职业四条
  //   skills.json5            技能五条
  //   quests.json5            任务四条
  //   crafting.json5          配方类别五条 + 配方九条
  //   subclasses.json5        副职六条（四条制作类各授予一条精通天赋）
  //   traits.json5            天赋四条（制作精通，对应四条制作副职）
  //   tags.json5              标签三条
  //   items.json5             物品二十九条
  //   damage_formulas.json5   伤害公式一条
  //   damage_categories.json5 伤害类别一条（lostland:fire；全局默认的
  //                           lostland:physical 由引擎注册，见
  //                           ll_mod::base_damage_category）
}
