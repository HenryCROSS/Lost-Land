// 迷途大陆本体内容 mod。
//
// # 这不是一个「示例」，是本体自己的游戏内容
//
// 项目所有者裁定「本体主要是把整个框架做好做完整，提供足够好和足够多
// 的功能和选项」——即 Rust 侧只留**能力**（注册表、契约解析、结算引擎、
// 值哈希），**实例**（哪个种族叫什么、属性修正是多少）全部落在这里。
// 本目录因此是「本体即 Mod」原则最后一块拼图：本体内容与第三方 mod
// 走的是同一条 `register-*` 脚本通道，没有任何本体专属的特权入口。
//
// # 这个目录不能删
//
// 与 `mods/example_mod/`（可有可无的演示）不同，本目录是**强制装载**
// 的：`ll_game::content::load_content` 在装载完毕后会按 id 逐字段解析
// `ll_mod::race::BaseRaceIds` 这类本体句柄结构体（`ll_mod::base_contract`
// 的契约解析机制），任何一条本体内容缺失都会让装载整批失败并点名缺了
// 哪些 id，而不是静默进到一个残破的游戏里。误删/改名本目录的表现是
// 启动期一条可行动的错误，不是十分钟后某个 NPC 生成不出来。
//
// # 为什么按内容类型拆多个入口文件
//
// `entry_points` 本来就是数组（见 `ll_mod::manifest::ModManifest`），
// 拆文件不引入任何机制，只是让「本体的种族都写在哪」有一个不需要
// 搜索就能回答的答案。后续批次迁移职业/技能/物品时各自新增一个文件，
// 不往同一个巨型脚本里堆。
{
  namespace: "lostland",
  version: "0.1.0",

  // 种族：迁移自 `ll_mod::race::materialize_base_races`（已删除）。
  // 职业：目前只有卫兵一条，理由见 classes.scm 文件头。
  // 制作类别：四条，都不设副职闸门，理由见 crafting.scm 文件头。
  // 副职：四条制作类副职 + 各自的使用计数获得条件。
  //
  // **顺序有意义**：subclasses.scm 里的 register-subclass-unlock 要求
  // trigger-target 指向的配方类别**已经注册**（只 get 不 intern），
  // 因此 crafting.scm 必须排在它前面。两者互换会在装载期报一条点名的
  // 错误——不会静默注册出一条谁都触发不了的获得条件。
  entry_points: ["races.scm", "classes.scm", "crafting.scm", "subclasses.scm"],
}
